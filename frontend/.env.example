# Backend API URL
# Leave empty when frontend and backend are deployed together on Vercel (same origin).
# For local dev, use http://localhost:5000 or leave empty to use the Vite proxy.
VITE_API_URL=http://localhost:5000
